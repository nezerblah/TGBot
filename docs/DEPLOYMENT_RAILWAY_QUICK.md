# Быстрый старт: Деплой на Railway за 5 минут

## ⚡ Самое важное (TL;DR)

### 1. Подготовка (1 минута)

```bash
# Убедитесь, что проект в Git репозитории
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/TGBot.git
git push -u origin main
```

### 2. Создание проекта на Railway (2 минуты)

1. Перейдите на https://railway.app
2. Нажмите **"Create New Project"** → **"Deploy from GitHub"**
3. Авторизуйте и выберите репозиторий `TGBot`
4. Railway создаст проект

### 3. Добавление PostgreSQL БД (1 минута)

1. В окне проекта нажмите **"+ Add"**
2. Выберите **"PostgreSQL"**
3. Railway автоматически добавит `DATABASE_URL` в переменные

### 4. Настройка переменных окружения (1 минута)

Перейдите в **"Variables"** и добавьте:

```env
BOT_TOKEN=123456789:ABCDefGHIjklmnoPQRstuvwxyzABCDefGH
ADMIN_ID=123456789
WEBHOOK_SECRET=your_super_secret_random_string_12345
ENVIRONMENT=production
```

**Узнать значения:**
- `BOT_TOKEN`: от @BotFather в Telegram
- `ADMIN_ID`: от @userinfobot в Telegram
- `WEBHOOK_SECRET`: любая случайная строка (генерируйте с помощью `openssl rand -hex 16`)

### 5. Получение URL приложения

1. Откройте **"Deployments"**
2. Найдите **"Public URL"** (например: `https://tg-bot-xxx.up.railway.app`)
3. Добавьте переменную:
   ```env
   WEBHOOK_URL=https://tg-bot-xxx.up.railway.app/webhook/your_super_secret_random_string_12345
   ```

### 6. Регистрация вебхука (30 секунд)

```bash
# Установите Railway CLI (если еще не установлен)
npm install -g @railway/cli

# Авторизуйтесь
railway login

# Свяжите проект
cd /Users/nezerblah/Documents/TGBot
railway link

# Установите вебхук
railway run python scripts/set_webhook.py
```

Или вручную через curl:
```bash
curl -X POST "https://api.telegram.org/bot{BOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://тут-url-приложения/webhook/secret",
    "secret_token": "secret"
  }'
```

## ✅ Готово!

Откройте Telegram, напишите боту сообщение - он должен ответить!

---

## 📋 Чек-лист

- [ ] Проект в GitHub
- [ ] Создан проект на Railway через GitHub
- [ ] PostgreSQL добавлена
- [ ] Все переменные окружения установлены
- [ ] Приложение статус "SUCCESS"
- [ ] Вебхук зарегистрирован
- [ ] Бот отвечает на сообщения

## 🆘 Если что-то не работает

1. **Приложение не запускается**: посмотрите логи в Railway (вкладка "Logs")
2. **Бот не отвечает**: проверьте вебхук командой `railway run python scripts/set_webhook.py`
3. **База данных ошибка**: убедитесь что `DATABASE_URL` установлена
4. **Планировщик не работает**: это нормально до запуска в 11:00 МСК

## 📞 Нужна помощь?

- [Документация Railway](https://docs.railway.app/)
- [Чит-лист Railway CLI](https://docs.railway.app/reference/cli-api)
- [Документация Telegram Bot API](https://core.telegram.org/bots/api)

**Удачи! 🚀**


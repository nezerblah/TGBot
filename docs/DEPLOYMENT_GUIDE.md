# Подробный гайд по деплою на Railway.com

## Содержание
1. [Подготовка](#подготовка)
2. [Создание проекта на Railway](#создание-проекта-на-railway)
3. [Конфигурация переменных окружения](#конфигурация-переменных-окружения)
4. [Создание базы данных](#создание-базы-данных)
5. [Деплой приложения](#деплой-приложения)
6. [Настройка вебхука Telegram](#настройка-вебхука-telegram)
7. [Проверка и мониторинг](#проверка-и-мониторинг)
8. [Решение проблем](#решение-проблем)

---

## Подготовка

### Что вам понадобится:
- Аккаунт на [railway.com](https://railway.com)
- GitHub аккаунт (для связи репозитория)
- Telegram Bot Token (от @BotFather)
- Git установлен на вашем компьютере
- Готовый исходный код проекта в Git репозитории

### Шаг 1: Убедитесь, что проект готов

1. **Все необходимые файлы присутствуют:**
   ```
   ✓ requirements.txt - содержит все зависимости
   ✓ Procfile - конфигурация для запуска
   ✓ .env.example - пример переменных окружения
   ✓ runtime.txt - версия Python
   ✓ railway.toml - конфигурация Railway
   ```

2. **Проверьте `requirements.txt`:**
   ```bash
   cat requirements.txt
   ```
   Убедитесь, что включены:
   - fastapi
   - uvicorn[standard]
   - SQLAlchemy
   - alembic
   - aiogram
   - python-dotenv
   - И другие зависимости

3. **Git репозиторий:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/TGBot.git
   git push -u origin main
   ```

---

## Создание проекта на Railway

### Шаг 2: Создание нового проекта

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите **"Create New Project"** (или **"Start a New Project"**)
3. Выберите один из вариантов:
   - **Deploy from GitHub** - рекомендуется для автоматического обновления
   - **Deploy from Docker image** - если у вас есть Dockerfile
   - **CLI** - для опытных пользователей

### Шаг 3: Подключение GitHub репозитория (рекомендуется)

1. Выберите **"Deploy from GitHub"**
2. Авторизуйте Railway доступ к вашему GitHub
3. Выберите ваш репозиторий `TGBot`
4. Railway автоматически создаст сервис
5. После добавления переменных окружения начнется автоматический деплой

### Шаг 3 (альтернатива): Использование Railway CLI

Если вы предпочитаете CLI:

```bash
# 1. Установите Railway CLI
npm install -g @railway/cli
# или используйте Homebrew на macOS
brew install railway

# 2. Авторизуйтесь
railway login

# 3. Создайте новый проект в Railway
railway init

# 4. Свяжите с текущей папкой
cd /Users/nezerblah/Documents/TGBot
railway link

# 5. Задайте имя сервиса (например: tg-bot-service)
```

---

## Конфигурация переменных окружения

### Шаг 4: Добавление переменных окружения

1. **В интерфейсе Railway:**
   - Откройте ваш проект
   - Перейдите на вкладку **"Variables"** или **"Environment"**
   - Нажмите **"Add Variable"**

2. **Добавьте следующие переменные:**

```env
# 1. Telegram Bot Token
BOT_TOKEN=123456789:ABCDefGHIjklmnoPQRstuvwxyzABCDefGH

# 2. ID администратора (ваш Telegram ID)
ADMIN_ID=123456789

# 3. Webhook Secret (генерируется автоматически)
WEBHOOK_SECRET=your_super_secret_random_string_here_12345678

# 4. URL будет определен позже (см. Шаг 7)
WEBHOOK_URL=https://[YOUR_RAILWAY_DOMAIN]/webhook/[WEBHOOK_SECRET]

# 5. Переменная окружения
ENVIRONMENT=production
```

**Как получить BOT_TOKEN:**
1. Откройте Telegram, найдите бота @BotFather
2. Напишите `/newbot`
3. Следуйте инструкциям
4. BotFather предоставит вам токен

**Как узнать ваш ADMIN_ID:**
1. Напишите боту @userinfobot сообщение
2. Он пришлет вам ваш ID

3. **Нажмите "Deploy"** после добавления каждой переменной

---

## Создание базы данных

### Шаг 5: Добавление PostgreSQL

1. **В главном окне проекта на Railway:**
   - Нажмите **"+ Add"** или **"Add Service"**
   - Выберите **"PostgreSQL"**
   - Railway автоматически создаст базу данных

2. **После создания БД:**
   - В переменные окружения автоматически добавится `DATABASE_URL`
   - Это будет выглядеть так:
     ```
     postgresql://username:password@host:5432/database_name
     ```

3. **Убедитесь, что переменная есть:**
   - Перейдите в Variables
   - Проверьте, что `DATABASE_URL` установлена

### Шаг 6: Инициализация базы данных

После деплоя вам нужно создать таблицы. Есть несколько способов:

**Способ 1: Через Procfile (рекомендуется)**

Убедитесь, что в вашем `Procfile` есть строка:
```
release: alembic upgrade head
```

Railway автоматически запустит эту команду перед стартом приложения.

**Способ 2: Вручную через Railway CLI**

```bash
railway run python -c "from app.db import Base, engine; from app import models; Base.metadata.create_all(bind=engine)"
```

**Способ 3: Через HTTP запрос**

После деплоя сделайте первый запрос к приложению:
```bash
curl https://your-railway-app.up.railway.app/
```

Это активирует приложение, и таблицы будут созданы (согласно коду в `main.py`).

---

## Деплой приложения

### Шаг 7: Получение URL приложения

1. **В интерфейсе Railway:**
   - Откройте ваш проект
   - Перейдите на вкладку **"Deployments"** или **"Settings"**
   - Найдите **"Public URL"** или **"Domain"**
   - Скопируйте URL, он будет выглядеть как:
     ```
     https://tg-bot-production-xxxx.up.railway.app
     ```

2. **Обновите переменную WEBHOOK_URL:**
   - Перейдите в Variables
   - Отредактируйте `WEBHOOK_URL`:
     ```
     WEBHOOK_URL=https://tg-bot-production-xxxx.up.railway.app/webhook/YOUR_WEBHOOK_SECRET
     ```
   - Сохраните изменения

3. **Railway автоматически переразвернет приложение**

### Шаг 8: Проверка статуса деплоя

1. **В интерфейсе Railway:**
   - Откройте вкладку **"Deployments"**
   - Проверьте статус текущего развертывания
   - Если статус "SUCCESS" - деплой прошел успешно
   - Если "FAILED" - посмотрите логи (Logs tab)

2. **Проверьте, что приложение работает:**
   ```bash
   curl https://your-railway-app.up.railway.app/
   ```
   
   Должен быть ответ:
   ```json
   {"ok":true}
   ```

---

## Настройка вебхука Telegram

### Шаг 9: Регистрация вебхука

После успешного деплоя нужно установить вебхук у Telegram. Есть два способа:

**Способ 1: Автоматический (рекомендуется)**

Создайте скрипт `setup_webhook.sh` в корне проекта:

```bash
#!/bin/bash

# Установите вебхук для Telegram
curl -X POST "https://api.telegram.org/bot${BOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "'${WEBHOOK_URL}'",
    "secret_token": "'${WEBHOOK_SECRET}'"
  }'

echo "Webhook set successfully!"
```

Затем запустите на Railway:
```bash
railway run bash setup_webhook.sh
```

**Способ 2: Через Python скрипт (уже есть в проекте)**

```bash
# Убедитесь, что переменные установлены
railway run python scripts/set_webhook.py
```

**Способ 3: Вручную через curl**

```bash
BOT_TOKEN="ваш_токен_здесь"
WEBHOOK_URL="https://ваш-домен.up.railway.app/webhook/ваш_secret"
WEBHOOK_SECRET="ваш_secret"

curl -X POST "https://api.telegram.org/bot${BOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "'${WEBHOOK_URL}'",
    "secret_token": "'${WEBHOOK_SECRET}'"
  }'
```

### Шаг 10: Проверка вебхука

```bash
BOT_TOKEN="ваш_токен_здесь"

# Получите информацию о текущем вебхуке
curl "https://api.telegram.org/bot${BOT_TOKEN}/getWebhookInfo"
```

Ожидаемый ответ:
```json
{
  "ok": true,
  "result": {
    "url": "https://your-railway-app.up.railway.app/webhook/your_secret",
    "has_custom_certificate": false,
    "pending_update_count": 0,
    "ip_address": "1.2.3.4",
    "last_error_date": null
  }
}
```

---

## Проверка и мониторинг

### Шаг 11: Проверка работоспособности

1. **Тестирование бота:**
   - Откройте Telegram
   - Напишите сообщение вашему боту
   - Бот должен ответить

2. **Проверка логов Railway:**
   - В интерфейсе Railway откройте вкладку **"Logs"**
   - Там должны быть логи обработки сообщений
   - Если есть ошибки, они будут там видны

3. **Проверка базы данных:**
   ```bash
   # Подключитесь к базе данных
   railway run psql $DATABASE_URL -c "SELECT * FROM users LIMIT 5;"
   ```

4. **Проверка планировщика:**
   - Подождите до 11:00 по МСК
   - Проверьте, получили ли подписчики гороскоп
   - Посмотрите логи в Railway

---

## Решение проблем

### Проблема: "502 Bad Gateway" или приложение не отвечает

**Решение:**
1. Проверьте переменные окружения (особенно `BOT_TOKEN` и `DATABASE_URL`)
2. Посмотрите логи: `railway run logs`
3. Перезапустите деплой: нажмите "Redeploy" в интерфейсе
4. Убедитесь, что приложение слушает на порту из переменной `$PORT`

### Проблема: Вебхук не работает

**Решение:**
1. Проверьте, что `WEBHOOK_URL` совпадает с реальным URL приложения
2. Убедитесь, что `WEBHOOK_SECRET` совпадает в переменных и в скрипте установки
3. Повторно установите вебхук: `railway run python scripts/set_webhook.py`
4. Проверьте логи: ищите сообщения об ошибках при получении обновлений

### Проблема: База данных не инициализируется

**Решение:**
1. Убедитесь, что `DATABASE_URL` задана в переменных окружения
2. Проверьте логи: `railway logs`
3. Вручную инициализируйте БД:
   ```bash
   railway run python -c "from app.db import Base, engine; from app import models; Base.metadata.create_all(bind=engine)"
   ```

### Проблема: Планировщик не запускается

**Решение:**
1. Убедитесь, что `setup_scheduler(bot)` вызывается при запуске приложения
2. Проверьте логи в Deployments
3. Убедитесь, что приложение работает 24/7 (Railway бесплатный план может заснуть)

### Проблема: Сообщения не доходят до пользователей

**Решение:**
1. Проверьте переменную `ADMIN_ID` и ID пользователей в БД
2. Убедитесь, что планировщик работает (см. выше)
3. Проверьте, что не истек токен бота
4. Посмотрите логи: `railway logs`

---

## Дополнительные команды Railway CLI

```bash
# Просмотр логов
railway logs

# Запуск команды в контексте Railway
railway run python scripts/set_webhook.py

# Просмотр переменных окружения
railway variables

# Открыть переменные в редакторе
railway variables --editor

# Остановить приложение
railway stop

# Перезапустить приложение
railway restart

# Просмотр информации о проекте
railway status

# Удалить проект
railway destroy
```

---

## Ссылки и ресурсы

- [Railway документация](https://docs.railway.app/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [aiogram документация](https://docs.aiogram.dev/)
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)

---

## Итоговая чек-лист деплоя

- [ ] Проект загружен на GitHub
- [ ] Создан аккаунт на railway.com
- [ ] Создан новый проект на Railway
- [ ] Добавлена PostgreSQL база данных
- [ ] Установлены все переменные окружения:
  - [ ] `BOT_TOKEN`
  - [ ] `ADMIN_ID`
  - [ ] `DATABASE_URL` (автоматически от PostgreSQL)
  - [ ] `WEBHOOK_SECRET`
  - [ ] `WEBHOOK_URL`
  - [ ] `ENVIRONMENT=production`
- [ ] Приложение успешно развернуто (статус "SUCCESS")
- [ ] Вебхук зарегистрирован в Telegram
- [ ] Бот отвечает на сообщения
- [ ] Планировщик отправляет гороскопы в 11:00 МСК
- [ ] Логи мониторятся регулярно

---

## Дополнительные советы

### Оптимизация для бесплатного плана Railway

1. **Автосна приложения:** Railway может усыпить приложение после периода неактивности. Используйте функцию "Always On" для платных планов или используйте внешний мониторинг.

2. **Оптимизация БД:** 
   - Используйте индексы на часто запрашиваемых полях
   - Регулярно удаляйте старые логи

3. **Логирование:**
   - Включите логирование ошибок
   - Используйте `sentry.io` для мониторинга ошибок в production

### Обновление приложения

После внесения изменений в код:

```bash
# Запушьте изменения в GitHub
git add .
git commit -m "Your commit message"
git push

# Railway автоматически переразвернет приложение
# Следите за статусом в интерфейсе Railway
```

Удачи с деплоем! 🚀


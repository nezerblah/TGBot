# 🎓 Шпаргалка по деплою на Railway (для нетерпеливых)

## 1️⃣ Подготовка GitHub (5 минут)

```bash
cd /Users/nezerblah/Documents/TGBot

# Если еще не git инициализирован:
git init
git add .
git commit -m "Initial commit"

# Добавить удаленный репозиторий
git remote add origin https://github.com/YOUR_USERNAME/TGBot.git
git branch -M main
git push -u origin main
```

## 2️⃣ Railway Dashboard (2 минуты)

**URL:** https://railway.app

```
Действия:
1. Sign up/Login через GitHub
2. Create New Project
3. Deploy from GitHub
4. Выбрать репозиторий TGBot
5. Дождаться создания проекта
```

## 3️⃣ Добавить PostgreSQL (1 минута)

```
1. В проекте нажать "+ Add"
2. Выбрать "PostgreSQL"
3. Дождаться инициализации
4. DATABASE_URL автоматически добавится
```

## 4️⃣ Переменные окружения (3 минуты)

**Перейти в:** Variables

```env
# Обязательно:
BOT_TOKEN=123456:ABC...
ADMIN_ID=123456789
WEBHOOK_SECRET=abc123def456...
ENVIRONMENT=production

# Опционально (может быть добавлена позже):
WEBHOOK_URL=https://тут-домен.up.railway.app/webhook/abc123def456...
```

**Где взять:**
- `BOT_TOKEN`: @BotFather → `/newbot`
- `ADMIN_ID`: @userinfobot → отправить сообщение
- `WEBHOOK_SECRET`: `openssl rand -hex 16`

## 5️⃣ Получить URL приложения (1 минута)

```
Перейти в: Deployments
Найти: Public URL
Скопировать: https://tg-bot-xxx.up.railway.app
```

## 6️⃣ Добавить WEBHOOK_URL (1 минута)

```
Variables → + Add Variable

WEBHOOK_URL=https://tg-bot-xxx.up.railway.app/webhook/abc123def456...

(где abc123def456... это ваш WEBHOOK_SECRET)
```

## 7️⃣ Установить вебхук (2 минуты)

### Способ A: Через Railway CLI

```bash
# 1. Установить
npm install -g @railway/cli

# 2. Авторизоваться
railway login

# 3. Перейти в папку проекта
cd /Users/nezerblah/Documents/TGBot

# 4. Привязать к проекту
railway link

# 5. Установить вебхук
railway run python scripts/set_webhook.py
```

### Способ B: Вручную через curl

```bash
BOT_TOKEN="ваш_токен"
WEBHOOK_URL="https://ваш-домен.up.railway.app/webhook/secret"
WEBHOOK_SECRET="secret"

curl -X POST "https://api.telegram.org/bot${BOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "'${WEBHOOK_URL}'",
    "secret_token": "'${WEBHOOK_SECRET}'"
  }'
```

## 8️⃣ Тестирование (2 минуты)

```bash
# 1. Проверить приложение
curl https://ваш-домен.up.railway.app/

# Ответ: {"ok":true}

# 2. Проверить вебхук
curl "https://api.telegram.org/bot${BOT_TOKEN}/getWebhookInfo"

# 3. Тестировать в Telegram
# - Откройте Telegram
# - Напишите боту любое сообщение
# - Бот должен ответить
```

---

## 🆘 Быстрые решения проблем

### "502 Bad Gateway"
```bash
railway logs
# Смотреть ошибки в логах
# Обычно: не заполнены переменные окружения
```

### Вебхук не установлен
```bash
# Повторить:
railway run python scripts/set_webhook.py

# Или проверить статус:
curl "https://api.telegram.org/bot${BOT_TOKEN}/getWebhookInfo"
```

### БД ошибка
```bash
# Проверить что DATABASE_URL есть:
railway variables

# Если нет - добавить PostgreSQL заново
```

### Бот не отвечает
```bash
# Смотреть логи:
railway logs

# Проверить вебхук:
curl "https://api.telegram.org/bot${BOT_TOKEN}/getWebhookInfo"

# Переустановить вебхук:
railway run python scripts/set_webhook.py
```

---

## 📊 Статус развертывания

```
В Deployments видите статус:
✅ SUCCESS    → Готово! Все работает
⏳ IN_PROGRESS → Ждите, развертывается
❌ FAILED     → Смотрите логи, что-то сломалось
```

---

## 💡 Полезные команды Railway CLI

```bash
# Просмотр логов реального времени
railway logs -f

# Просмотр переменных
railway variables

# Запуск команды
railway run python scripts/set_webhook.py

# Перезапуск приложения
railway restart

# Статус проекта
railway status

# Подключение к БД
railway run psql $DATABASE_URL
```

---

## ✅ Финальный чек-лист

- [ ] GitHub репозиторий создан и есть коммиты
- [ ] Проект создан на Railway
- [ ] PostgreSQL добавлена
- [ ] Все 4 переменные установлены (BOT_TOKEN, ADMIN_ID, WEBHOOK_SECRET, ENVIRONMENT)
- [ ] Приложение развернуто (статус SUCCESS)
- [ ] Получен Public URL
- [ ] WEBHOOK_URL добавлена
- [ ] Вебхук установлен (через скрипт или curl)
- [ ] Бот отвечает на сообщения в Telegram

---

## ⏱️ Общее время: ~15-20 минут

Если что-то займет дольше:
1. Посмотрите логи Railway
2. Проверьте переменные окружения
3. Повторите установку вебхука
4. Прочитайте [`DEPLOYMENT_CHECKLIST.md`](./DEPLOYMENT_CHECKLIST.md)

---

## 🔗 Важные ссылки

| Ссылка | Назначение |
|--------|-----------|
| https://railway.app | Railway Dashboard |
| https://t.me/botfather | Создание бота |
| https://t.me/userinfobot | Узнать свой ID |
| https://docs.railway.app | Документация Railway |
| https://core.telegram.org/bots/api | Telegram Bot API |

---

**Готовы? Начните с шага 1️⃣ и следуйте инструкциям!** 🚀

_Если где-то застрял - смотри раздел 🆘 "Быстрые решения проблем"_


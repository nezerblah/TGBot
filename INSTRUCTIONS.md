# 🛠 Инструкция для владельца бота

Полное руководство по настройке, развёртыванию и администрированию.

---

## 📋 Содержание

1. [Что нужно перед стартом](#1-что-нужно-перед-стартом)
2. [Создание бота в Telegram](#2-создание-бота-в-telegram)
3. [Деплой на Railway (пошагово)](#3-деплой-на-railway-пошагово)
4. [Переменные окружения](#4-переменные-окружения)
5. [Настройка Webhook](#5-настройка-webhook)
6. [Команды администратора](#6-команды-администратора)
7. [Приём платежей (Telegram Stars)](#7-приём-платежей-telegram-stars)
8. [Локальный запуск для разработки](#8-локальный-запуск-для-разработки)

---

## 1. Что нужно перед стартом

- Аккаунт в Telegram
- Аккаунт на [Railway](https://railway.app) (бесплатный план работает)
- Аккаунт на [GitHub](https://github.com) (для хранения кода)
- 10 минут времени

---

## 2. Создание бота в Telegram

### Шаг 1 — Создать бота через BotFather

1. Откройте Telegram, найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Введите **название бота** (например: `Мой Таро Бот`)
4. Введите **username бота** — должен заканчиваться на `bot` (например: `my_tarot_bot`)
5. BotFather пришлёт **токен** вида: `1234567890:AAFxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
6. **Сохраните токен** — он понадобится на следующем шаге

### Шаг 2 — Узнать свой Telegram ID (для прав администратора)

1. Найдите бота [@userinfobot](https://t.me/userinfobot) или [@getmyid_bot](https://t.me/getmyid_bot)
2. Отправьте `/start`
3. Бот пришлёт ваш числовой ID — **запишите его** (например: `123456789`)

### Шаг 3 — Включить платежи Telegram Stars (если нужны подписки)

1. В BotFather: `/mybots` → выбрать вашего бота → **Payments**
2. Выбрать **Telegram Stars** как провайдер
3. Подтвердить — теперь бот может принимать звёзды

---

## 3. Деплой на Railway (пошагово)

### Шаг 1 — Загрузить код на GitHub

1. Создайте новый репозиторий на GitHub (можно приватный)
2. Загрузите все файлы проекта в репозиторий

### Шаг 2 — Создать проект на Railway

1. Зайдите на [railway.app](https://railway.app) и войдите через GitHub
2. Нажмите **New Project**
3. Выберите **Deploy from GitHub repo**
4. Выберите ваш репозиторий с кодом бота
5. Railway автоматически обнаружит Python-проект

### Шаг 3 — Добавить хранилище данных (Volume)

1. В проекте Railway нажмите **+ Add Service** → **Volume**
2. Назовите том `data`
3. Укажите путь монтирования: `/data`
4. Это нужно для сохранения базы данных SQLite между деплоями

### Шаг 4 — Задать переменные окружения

В Railway: ваш сервис → вкладка **Variables** → добавить каждую переменную:

```
BOT_TOKEN        = 1234567890:AAFxxxxxxxxxxxxxxxx   ← токен от BotFather
ADMIN_ID         = 123456789                         ← ваш Telegram ID
WEBHOOK_SECRET   = придумайте_любую_строку_32символа ← любой случайный текст
DATA_DIR         = /data                             ← путь к Volume
```

> **WEBHOOK_URL** будет доступен после первого деплоя — см. Шаг 6.

### Шаг 5 — Первый деплой

1. Railway автоматически задеплоит бота при добавлении переменных
2. Дождитесь зелёного статуса в разделе **Deployments**
3. Перейдите в раздел **Settings** → **Networking** → скопируйте **Public URL**
   - Выглядит как: `https://ваш-бот.railway.app`

### Шаг 6 — Добавить WEBHOOK_URL и настроить Webhook

1. В Variables добавьте:
   ```
   WEBHOOK_URL = https://ваш-бот.railway.app/webhook
   ```
   (замените `ваш-бот.railway.app` на ваш реальный домен)

2. Сделайте редеплой: **Deployments** → **Redeploy**

3. Настройте Webhook — откройте в браузере:
   ```
   https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=https://ваш-бот.railway.app/webhook&secret_token=<WEBHOOK_SECRET>
   ```
   Замените `<BOT_TOKEN>` и `<WEBHOOK_SECRET>` на ваши значения.

4. Должен вернуться ответ: `{"ok":true,"result":true}`

### Шаг 7 — Проверить работу

1. Откройте Telegram, найдите вашего бота по username
2. Отправьте `/start`
3. Должно прийти приветствие и появиться кнопки меню ✅

---

## 4. Переменные окружения

| Переменная | Обязательная | Описание | Пример |
|---|---|---|---|
| `BOT_TOKEN` | ✅ | Токен бота от BotFather | `1234567890:AAFxxx...` |
| `ADMIN_ID` | ✅ | Ваш числовой Telegram ID | `123456789` |
| `WEBHOOK_SECRET` | ✅ | Секрет для проверки запросов Telegram | `my_secret_key_32chars` |
| `WEBHOOK_URL` | ✅ | Полный URL вебхука вашего деплоя | `https://xxx.railway.app/webhook` |
| `DATA_DIR` | Рекомендуется | Путь к постоянному хранилищу | `/data` |

> ⚠️ **Никогда не публикуйте `BOT_TOKEN` и `WEBHOOK_SECRET` в открытый репозиторий!**

---

## 5. Настройка Webhook

Webhook — механизм, через который Telegram отправляет сообщения боту.

### Установить Webhook вручную

```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=<WEBHOOK_URL>&secret_token=<WEBHOOK_SECRET>"
```

### Проверить текущий Webhook

```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
```

### Удалить Webhook (если нужно)

```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/deleteWebhook"
```

---

## 6. Команды администратора

Все команды доступны **только** аккаунту с `ADMIN_ID`. Обычные пользователи их не видят.

Полный список — отправьте боту:
```
/help_adminxq
```

### Доступные команды

| Команда | Описание |
|---|---|
| `/help_adminxq` | Справка по всем admin-командам |
| `/stats` | Статистика: пользователи, подписки, запросы |
| `/subscribers` | Подробно по подпискам на гороскопы (по знакам) |
| `/broadcast_all` | Рассылка сообщения **всем** пользователям бота |
| `/broadcast_free` | Рассылка пользователям **без Premium/Premium+** |
| `/send_now` | Принудительно запустить рассылку гороскопов |
| `/cancel` | Отменить текущую рассылку |

### Как сделать рассылку

1. Отправьте `/broadcast_all` (всем) или `/broadcast_free` (без подписки)
2. Бот спросит: _"Введите текст сообщения"_
3. Отправьте текст — бот разошлёт всем и отчитается:
   ```
   📢 Рассылка завершена.
   ✅ Доставлено: 142
   ❌ Ошибки: 3
   ```
4. Для отмены на шаге 2 — отправьте `/cancel`

### Статистика `/stats`

Показывает:
- 👥 Всего пользователей
- ⭐ Активных Premium подписок
- 💎 Активных Premium+ подписок
- 🌙 Подписок на ежедневную карту Таро
- ♈ Подписок на гороскопы по знакам
- 📨 Количество запросов за 24ч / 7 дней / 30 дней

### Привилегии администратора

Администратор получает **полный безлимит** на все функции бота:
- Безлимитные быстрые предсказания (не считаются в лимит)
- Доступ ко всем сложным раскладам без подписки и оплаты

---

## 7. Приём платежей (Telegram Stars)

### Как работает

- Пользователь платит звёздами прямо в Telegram
- Звёзды зачисляются на баланс **вашего Telegram-аккаунта** (владельца бота)
- Telegram удерживает ~30% комиссии

### Где смотреть баланс

1. Откройте [@BotFather](https://t.me/BotFather)
2. `/mybots` → выбрать бота → **Payments**
3. Там отображается накопленный баланс звёзд

### Вывод средств

1. Минимум для вывода: ~1000 ⭐
2. Вывод через [Fragment](https://fragment.com) — маркетплейс Telegram
3. Fragment → TON → любой криптообменник или P2P

### Текущие цены в боте

| Продукт | Цена |
|---|---|
| Premium (30 дней) | 10 ⭐ |
| Premium+ (30 дней) | 100 ⭐ |
| Разовый расклад | 15 ⭐ |

Чтобы изменить цены — отредактируйте константы в `app/payments.py`:
```python
PREMIUM_PRICE_STARS = 10        # цена Premium
PREMIUM_PLUS_PRICE_STARS = 100  # цена Premium+
SPREAD_SINGLE_PRICE_STARS = 15  # цена разового расклада
```

---

## 8. Локальный запуск для разработки

### Требования

- Python 3.11+
- pip

### Установка

```bash
# Клонировать репозиторий
git clone https://github.com/ваш-username/ваш-репо.git
cd ваш-репо

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

### Создать файл .env

Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=1234567890:AAFxxxxxxxxxxxxxxxx
ADMIN_ID=123456789
WEBHOOK_SECRET=local_dev_secret
WEBHOOK_URL=https://xxxx.ngrok.io/webhook
```

> Для локальной разработки пробросьте порт через [ngrok](https://ngrok.com):
> ```bash
> ngrok http 8000
> # скопируйте URL вида https://xxxx.ngrok.io → используйте как WEBHOOK_URL
> ```

### Запуск

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### Тесты

```bash
pytest -q
```

### Проверка стиля кода

```bash
black --check --line-length 120 .
ruff check . --select E,F,W,I --ignore E501
mypy app/ --ignore-missing-imports --no-strict-optional
```

---

## 🗂 Структура проекта

```
app/
├── main.py          # FastAPI приложение, инициализация
├── bot.py           # Инициализация бота, команды меню
├── handlers.py      # Вся логика обработки сообщений
├── keyboards.py     # Клавиатуры и кнопки
├── models.py        # Модели базы данных (SQLAlchemy)
├── db.py            # Подключение к БД, миграции
├── payments.py      # Логика подписок и платежей
├── scheduler.py     # Ежедневные рассылки (APScheduler)
├── tarot.py         # Колода карт Таро
├── astro_parser.py  # Парсер раскладов с astrocentr.ru
├── webhook.py       # Endpoint для Telegram webhook
├── rate_limit.py    # Защита от флуда
└── horo/
    └── parser.py    # Парсер гороскопов
tests/               # Тесты (pytest)
scripts/             # Вспомогательные скрипты
```

---

## ❓ Частые проблемы

**Бот не отвечает после деплоя**
→ Проверьте, что Webhook установлен (раздел 5). Логи: Railway → **Deployments** → **View Logs**.

**Ошибка "BOT_TOKEN is not set"**
→ Проверьте, что переменная `BOT_TOKEN` добавлена в Railway → Variables.

**База данных сбрасывается после деплоя**
→ Убедитесь, что Volume подключён и `DATA_DIR=/data` задан в переменных.

**Платежи не проходят**
→ Убедитесь, что в BotFather включены Telegram Stars Payments для вашего бота.

**Команды администратора не работают**
→ Проверьте, что `ADMIN_ID` в Variables совпадает с вашим реальным Telegram ID (узнать через [@userinfobot](https://t.me/userinfobot)).


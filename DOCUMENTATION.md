# 📚 Полная документация TGBot

## 📖 Содержание

1. **README.md** - Основная информация о боте
2. **IMPROVEMENTS.md** - Детальное описание всех улучшений
3. **DEVELOPMENT_GUIDE.md** - Гайд по разработке новых функций
4. **FAQ.md** - Часто задаваемые вопросы и решения проблем
5. **TGBot/.env.example** - Шаблон конфигурации
6. **TGBot/requirements.txt** - Список зависимостей
7. **TGBot/main.py** - Основной код бота

---

## 🎯 Быстрый старт

### 1. Установка

```bash
# Клонируйте или скачайте проект
cd TGBot

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Конфигурация

```bash
# Скопируйте пример конфигурации
cp TGBot/.env.example TGBot/.env

# Отредактируйте .env
# nano TGBot/.env
# или используйте ваш редактор
```

### 3. Запуск

```bash
python TGBot/main.py
```

---

## 🏗️ Архитектура проекта

### Слои приложения

```
┌─────────────────────────────────────────┐
│  Пользовательские команды               │
│  /start, /list, /mysubs                 │
└────────────┬────────────────────────────┘
             │
┌────────────▼────────────────────────────┐
│  Обработчики кнопок (Callbacks)         │
│  button() - маршрутизация               │
└────────────┬────────────────────────────┘
             │
┌────────────▼────────────────────────────┐
│  Бизнес-логика                          │
│  parse_horoscope, subscribe_user        │
└────────────┬────────────────────────────┘
             │
┌────────────▼────────────────────────────┐
│  Вспомогательные функции (_prefix)      │
│  _safe_send_message, _truncate_message  │
└────────────┬────────────────────────────┘
             │
┌────────────▼────────────────────────────┐
│  Слой данных                            │
│  SQLite БД, API запросы                 │
└─────────────────────────────────────────┘
```

### Модули

| Модуль | Ответственность |
|--------|-----------------|
| `logging` | Логирование всех операций |
| `sqlite3` | Хранилище подписок |
| `requests` | Получение гороскопов |
| `bs4` | Парсинг HTML/JSON |
| `telegram` | API Telegram |
| `pytz` | Работа с таймзонами |

---

## 🔄 Поток данных

### Подписка пользователя

```
1. Пользователь нажимает /start
   ↓
2. bot.start() отправляет клавиатуру со знаками
   ↓
3. Пользователь нажимает кнопку "Овен"
   ↓
4. button() обрабатывает callback (action="sub", sign="aries")
   ↓
5. subscribe_user(user_id, "aries") сохраняет в БД
   ↓
6. parse_horoscope("aries") получает текст гороскопа
   ↓
7. _format_horoscope_message() форматирует сообщение
   ↓
8. _safe_send_message() отправляет пользователю
```

### Ежедневная рассылка

```
1. Job queue запускает daily_horoscope() в 16:15 MSK
   ↓
2. get_subscribers() получает всех подписчиков из БД
   ↓
3. Для каждого знака:
   - parse_horoscope() получает текст
   - _format_horoscope_message() форматирует
   - _safe_send_message() отправляет каждому пользователю
   ↓
4. Логируются все ошибки и успехи
```

---

## 📊 Структура БД

### Таблица: subscriptions

```sql
CREATE TABLE subscriptions (
    user_id INTEGER NOT NULL,
    zodiac_slug TEXT NOT NULL,
    PRIMARY KEY (user_id, zodiac_slug)
)
```

**Примеры данных:**
```
user_id    | zodiac_slug
-----------|-------------
123456789  | aries
123456789  | taurus
987654321  | leo
```

**Запросы:**

```python
# Получить все подписки пользователя
SELECT zodiac_slug FROM subscriptions WHERE user_id = 123456789

# Получить всех подписчиков знака
SELECT user_id FROM subscriptions WHERE zodiac_slug = 'aries'

# Получить статистику
SELECT zodiac_slug, COUNT(user_id) FROM subscriptions GROUP BY zodiac_slug
```

---

## 🔐 Безопасность

### Что обработано

✅ **Валидация входных данных**
- Проверка знаков зодиака перед использованием
- Проверка типов данных

✅ **Обработка ошибок**
- Try-except вокруг всех критических операций
- Логирование всех исключений
- Graceful degradation

✅ **Размер сообщений**
- Автоматическая обрезка >4000 символов
- Telegram API лимит: 4096 символов

✅ **Права доступа**
- Проверка ADMIN_ID для админ-команд
- Пользователи видят только свои подписки

### Что НЕ обработано (security considerations)

⚠️ **SQL Injection** - используются параметризованные запросы
⚠️ **Rate limiting** - Telegram API имеет встроенные лимиты
⚠️ **Аутентификация** - только Telegram ID используется
⚠️ **Шифрование** - БД в открытом виде (используйте encryption на production)

---

## 🧪 Тестирование

### Ручное тестирование

```bash
# 1. Запустите бота
python TGBot/main.py

# 2. В Telegram найдите вашего бота
# 3. Нажмите /start
# 4. Подпишитесь на знак
# 5. Проверьте /list и /mysubs
# 6. Проверьте отписку
```

### Тестирование функций БД

```python
# В интерпретаторе Python
from TGBot.main import *

init_db()
subscribe_user(123456, 'aries')
print(get_user_subscriptions(123456))
print(get_subscribers())
unsubscribe_user(123456)
```

### Тестирование парсинга

```python
from TGBot.main import parse_horoscope

text, rates = parse_horoscope('aries')
print(f"Length: {len(text)}")
print(f"Rates: {rates}")
```

---

## 📈 Производительность

### Оптимизации

- ✅ Асинхронная обработка (async/await)
- ✅ Пулинг вместо вебхуков (simpler, но медленнее)
- ✅ Кеширование гороскопов в памяти (в daily_horoscope)
- ✅ Минимальные зависимости (13 пакетов)

### Масштабирование

Для 10k+ пользователей:

```python
# 1. Добавьте индексы в БД
conn.execute("CREATE INDEX idx_user_id ON subscriptions(user_id)")

# 2. Используйте Redis для кеша
import redis
cache = redis.Redis()

# 3. Разделите рассылку на батчи
# batch_size = 100
# for i in range(0, len(user_ids), batch_size):
#     await send_batch(user_ids[i:i+batch_size])

# 4. Используйте webhook вместо polling
# app = ApplicationBuilder().token(TOKEN).updater(None).build()
# async with app:
#     await app.bot.set_webhook(url="https://yourserver.com/webhook")
```

---

## 🚀 Deployment

### На Railway (рекомендуется)

```bash
# 1. Создайте аккаунт на railway.app
# 2. Подключите репозиторий GitHub
# 3. Добавьте переменные окружения в Railway dashboard:
#    TELEGRAM_BOT_TOKEN=...
#    ADMIN_ID=...
# 4. Railway автоматически запустит main.py
```

### На VPS (Ubuntu 20.04+)

```bash
# 1. SSH на сервер
ssh user@server.com

# 2. Установите Python и зависимости
sudo apt update && sudo apt install python3-pip python3-venv

# 3. Скопируйте проект
git clone https://github.com/yourrepo/TGBot.git
cd TGBot

# 4. Создайте окружение
python3 -m venv venv
source venv/bin/activate
pip install -r TGBot/requirements.txt

# 5. Создайте systemd сервис
sudo nano /etc/systemd/system/tgbot.service
```

**tgbot.service:**
```ini
[Unit]
Description=Telegram Horoscope Bot
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/home/youruser/TGBot
Environment="PATH=/home/youruser/TGBot/venv/bin"
ExecStart=/home/youruser/TGBot/venv/bin/python TGBot/main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
# 6. Активируйте сервис
sudo systemctl enable tgbot
sudo systemctl start tgbot
sudo systemctl status tgbot

# 7. Просмотрите логи
sudo journalctl -u tgbot -f
```

---

## 🛠️ Troubleshooting

Частые проблемы и решения см. в **FAQ.md**

---

## 📞 Контакты и поддержка

- 📚 Документация: [python-telegram-bot](https://docs.python-telegram-bot.org/)
- 🐛 Issues: GitHub Issues
- 💬 Discussion: GitHub Discussions

---

## 📋 Лицензия

MIT License - свободное использование

---

## 🎓 Обучающие материалы

Структура этого бота может быть использована для обучения:

- **Async Python** - весь код асинхронный
- **API интеграция** - парсинг и работа с API
- **Telegram Bot API** - работа с webhook/polling
- **SQLite** - работа с БД
- **Логирование** - практика логирования
- **Обработка ошибок** - patterns обработки исключений

---

**Версия документации:** 2.0  
**Обновлено:** 15.02.2026  
**Статус:** ✅ Готово к production

**Спасибо за внимание! 🚀**


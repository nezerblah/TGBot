# 🔧 ИСПРАВЛЕНИЕ ОШИБКИ PROCFILE

## ❌ Проблема

```
python: can't open file '/app/TGBot/main_webhook.py': [Errno 2] No such file or directory
```

**Причина:** Procfile находится в неправильном месте (в папке TGBot вместо корня)

---

## ✅ Решение

### Шаг 1: Удалить неправильный Procfile

```bash
cd ~/PycharmProjects/TGBot

# Удалить Procfile из папки TGBot
rm TGBot/Procfile

# Проверить что он в корне
ls -la Procfile
cat Procfile
```

**Правильный результат:**
```
web: python TGBot/main_webhook.py
```

### Шаг 2: Закоммитить в git

```bash
git add Procfile
git rm TGBot/Procfile  # Удалить из git
git commit -m "Fix: Move Procfile to root directory"
git push origin main
```

### Шаг 3: Railway перезагрузит сам

- Railway увидит изменения
- Автоматически перезапустит бота
- Проверить логи: `railway logs -f`

---

## 📍 Правильная структура

```
TGBot/ (корень проекта)
├── Procfile ← ЗДЕСЬ (в корне!)
├── TGBot/
│   ├── main_webhook.py
│   ├── main.py
│   ├── requirements.txt
│   └── .env
├── README.md
├── WEBHOOK_QUICKSTART.md
└── ... (другие документы)
```

---

## ✨ После этого будет работать!

Railway найдет Procfile в корне → прочитает `web: python TGBot/main_webhook.py` → запустит правильно → БОТ РАБОТАЕТ! ✅

---

**Это займет 2 минуты и исправит проблему! 🚀**

